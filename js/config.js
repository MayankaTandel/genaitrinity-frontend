// Point this at wherever the backend is running.
// Local dev default: the backend's "npm start" runs on port 3000.
// When you deploy the backend (Render/Railway/etc.), change this to that
// deployed URL, e.g. "https://vital-api.onrender.com/api".
const API_BASE_URL = "http://localhost:3000/api";
